<?xml version="1.0" encoding="utf-8"?>
<resources>
    <bool name="zoom_level_switch_supported">false</bool>
    <bool name="allow_logical_camera_ids">false</bool>

    <!-- An array of (0: back camera / 1: front camera, approximate zoom ratio).
     These values will be added to the lens switcher if one or more cameras uses the
     multi-camera API to let the user select which lens to use. It's necessary to set
     switch_in_zoom_level_supported to true. You also need to find the exact zoom
     ratio needed to trigger the lens switch from your stock camera app and observing
     the zoom ratio. Example for Google Pixel 4A 5G back camera configuration:
         <string-array name="config_auxCameraZoomRatios">
             <item>0:1.0</item>
             <item>0:0.62</item>
         </string-array>
    -->
    <string-array name="config_auxCameraZoomRatios">
        <!-- <item>0:1.0</item> -->
    </string-array>
</resources>
